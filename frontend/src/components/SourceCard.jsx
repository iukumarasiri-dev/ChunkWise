// Shows one retrieved chunk behind an answer: filename, page number, excerpt,
// and similarity score.
// TODO: render props { filename, page, excerpt, score }.

export default function SourceCard() {
  return null;
}
