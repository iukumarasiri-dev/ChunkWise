// Renders the sequence of question/answer turns. Each answer is followed by its
// SourceCard list.
// TODO: map over messages; render SourceCard for each answer's sources.

export default function ChatThread() {
  return null;
}
