// Upload page: drop a PDF/DOCX, then watch its ingestion status and chunk count
// in the document list.
// TODO: file input + DocumentList, wired to api/client.

export default function UploadPage() {
  return <div>Upload</div>;
}
