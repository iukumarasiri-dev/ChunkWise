// Chat page: ask a question (optionally scoped to one document) and see the
// grounded answer with its source chunks.
// TODO: ChatThread + question input + document scope selector.

export default function ChatPage() {
  return <div>Chat</div>;
}
