"""Split parsed page text into overlapping chunks.

Chunk size and overlap come from config. Each chunk keeps its source file and
page number for citation.
"""

# TODO: chunk_pages(pages, size, overlap) -> list[Chunk]
